{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ -d \"frontend\" ]; then cd frontend; fi && npm run build",
  "installCommand": "if [ -d \"frontend\" ]; then cd frontend; fi && npm install --include=dev"
}
